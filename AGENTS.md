# Mac Sensor Lab Agent 规则

## 目标

本项目用于构建一个原生、快速、事实优先的 macOS 传感器查看与实验工具。工作名为 **Mac Sensor Lab**。

## 安全边界

- 默认只修改本项目目录。
- 不修改系统设置、隐私权限、风扇策略、功耗策略、启动项或网络配置。
- 不运行整个 GUI App 为 root，不把密码写入命令、脚本、日志或文件。
- 需要密码、Touch ID、管理员权限、麦克风、定位或其他隐私授权时，停在系统提示前交给用户。
- 对传感器只做读取；控制型功能不属于当前 MVP。
- 不收集、保存或导出序列号、UDID、Hardware UUID、用户名、主机名、精确位置或其他设备唯一标识。
- 不删除项目外文件。项目内删除、覆盖或批量迁移也应先核验精确目标。

## 数据真实性

- 原始值、校准值、估算值和派生值必须明确区分。
- 未经校准的环境光不得标为 lux；数字音频 dBFS 不得标为 dBA；内部温度不得称为室温。
- 不支持、无权限、读取超时和设备缺失必须显示为状态，不得用模拟值冒充真实读数。
- 私有或逆向接口必须标明来源、兼容范围和不稳定性。

## 上游代码

- 复用前核验许可证、固定 commit，并在 `references/upstreams.md` 记录。
- 无明确许可证的仓库只能研究思路，不复制代码、资源或文案。
- 第三方仓库仅浅克隆到 `.work/upstream/`；不把嵌套 Git 仓库提交进正式源码。
- 任何移植代码保留必要版权与许可证声明。

## 实现与验证

- UI 外壳应先出现；慢速 Provider 在后台加载并提供明确进度/状态。
- 每个传感器通过独立 Provider 接入，失败不得拖垮其他模块。
- 运行最相关的 Swift 测试、CLI 自检和 App 启动验证。
- 构建和测试产物放 `.build/`、`.work/` 或 `outputs/`，不污染源码目录。

## 发布边界

- 公开前必须完成许可证、隐私、Git 历史、跟踪文件和本机路径审计。
- GitHub Actions 默认仅允许手动触发；日常验证优先使用 `scripts/verify-local.sh`。
- 未经 Developer ID 签名和公证的 App 不作为面向普通用户的可下载二进制发布。
